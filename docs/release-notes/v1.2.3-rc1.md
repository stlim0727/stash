# Release Notes — v1.2.3-rc1

**Target Build**: `v1.2.3-rc1 @ 44ed7ab`

### 🐛 Fixes
- [ ] **Preserve trash state on sync creates (Sentry 5a)**: Verify that bookmarks trashed before gaining a remote ID (e.g. captured offline or pending initial upload) stay trashed after syncing to the cloud rather than resurrecting as active bookmarks. (#711)
- [ ] **Fix library reset / sync-paused busy state race**: Verify resetting library during an active sync gracefully handles busy guards. (#712)
- [ ] **Skip AI trigger for skip-policy imports (STASH-59)**: Verify bulk imports with `enrichment_policy='skip'` do not enqueue `pendingAiTrigger`. (#710)
- [ ] **Preserve enrichment_policy in bulk create upload payload (STASH-59)**: Verify bulk imports carry `enrichment_policy` in payloads during creation. (#709)
- [ ] **Batch tag/collection persistence for bulk-import**: Verify bulk import performance with batched pending tag/collection ops. (#708)
- [ ] **Sequentialize account-transition drop fan-out (STASH-57)**: Verify account switching handles cleanup sequentially without SQLite lock contention. (#707)
