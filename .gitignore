# Dependencies
node_modules/
.pnpm-store/
package-lock.json

# Environment files
.env
.env.*
!.env.example

# Build outputs
.expo/
dist/
build/
coverage/

# Netlify CLI local state — legacy web-preview host, replaced by Cloudflare
# Workers preview URLs (wrangler.toml `preview_urls`; see the web-preview skill).
# Kept ignored so an ad-hoc `netlify` CLI run can't litter the tree.
.netlify/
**/.netlify/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# OS/editor files
.DS_Store
.vscode/
.idea/

# Downloaded fonts for scripts/generate-wordmark.mjs
scripts/.wordmark-cache/
.claude/worktrees/
.claude/settings.local.json

# Local agent/runtime artifacts
.codex-remote-attachments/
scratch/
tmp/

# ui-preview skill output (local scratch images, not source)
scratchpad/

# AntiCrow ローカルデータ
.anticrow/
